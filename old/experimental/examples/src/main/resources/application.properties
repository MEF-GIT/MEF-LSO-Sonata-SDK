# ===============================
# = META
# ===============================
application.version=1.0.0

# ===============================
# = CONTAINER CONTEXT Root
# ===============================
#server.context-path=/bss

# ===============================
# = DATA SOURCE
# ===============================

# Set here configuration for the database connection

# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle=true
spring.datasource.validationQuery=SELECT 1

# ===============================
# = JPA / HIBERNATE
# ===============================

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager).

# Show or not log for each sql query
spring.jpa.show-sql=true

# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated accordingly to java entities found in
# the project
spring.jpa.hibernate.ddl-auto=update

# Naming strategy
spring.jpa.hibernate.naming-strategy=org.hibernate.cfg.ImprovedNamingStrategy

# Allows Hibernate to generate SQL optimized for a particular DBMS
spring.jpa.properties.hibernate.dialect=com.orange.soa.commons.jpa.jdbc.ExtendedMySQLDialect

#spring.jpa.properties.hibernate.enable_lazy_load_no_trans=true

# ===============================
# = CORS
# ===============================

#CORS Support - set allowed origins separated by commas (for example see https://github.com/iainporter/oauth2-provider-test-client)
cors.allowed.origins=*

# ===============================
# = HASH ID
# ===============================
hashid.salt=aGFuc2hvdGZpcnN0

serviceorder.url=
serviceorder.authorization=

#spring.datasource.url=jdbc:mysql://localhost:3306/onapbss
#spring.datasource.driver-class-name=com.mysql.jdbc.Driver
## Username and password
#spring.datasource.username=root
#spring.datasource.password=
